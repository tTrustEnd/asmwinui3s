<Page
    x:Class="AsignmentWinUI.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" HorizontalAlignment="Center" VerticalAlignment="Center" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10">
            <TextBlock
            Text="Name: "
            Style="{StaticResource PageTitleStyle}" />
            <TextBox Height="30" Width="100"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Button Grid.Row="1" Content="Join" Width="180"
                    Command="{x:Bind ViewModel.JoinChatPageCommand}"
                    />
        </Grid>
        
    </Grid>
</Page>
